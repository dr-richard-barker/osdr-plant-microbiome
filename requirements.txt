# OSDR Plant Microbiome FAIR tool — runtime dependencies
pandas>=2.0
numpy>=1.24
plotly>=5.20
matplotlib>=3.7      # static publication figures (PNG + SVG)
networkx>=3.0        # knowledge graph + study-similarity projection
scikit-learn>=1.3    # semi-supervised guild inference (LabelSpreading, RandomForest)
scipy>=1.10          # Spearman co-occurrence network
# stdlib used: sqlite3, urllib, json, pathlib (no extra install needed)
